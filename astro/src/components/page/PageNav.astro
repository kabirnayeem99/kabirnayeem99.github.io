---
import type { MainNavItem } from "../../lib/main-nav";

interface Props {
  readonly items: readonly MainNavItem[];
  readonly ariaLabel?: string;
}

const { items, ariaLabel = "Primary" } = Astro.props as Props;
const firstHref = items[0]?.href ?? "";
const kicker = firstHref.includes("ar")
  ? "اِسْتَكْشِفْ"
  : firstHref.includes("ur")
    ? "دریافت کریں"
    : "Explore";
---

<nav class="page-nav" aria-label={ariaLabel}>
  <p class="page-nav-kicker" aria-hidden="true">{kicker}</p>
  <div class="page-nav-track">
    {
      items.map((item) => (
        <a
          class="page-nav-link"
          href={item.href}
          aria-current={item.isCurrent ? "page" : undefined}
        >
          {item.label}
        </a>
      ))
    }
  </div>
</nav>
